meta {
  name: Command
  type: http
  seq: 2
}

post {
  url: http://127.0.0.1:8000/api/ami/command
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "action": "CoreShowChannels"
  }
}

settings {
  encodeUrl: true
  timeout: 15000
}
