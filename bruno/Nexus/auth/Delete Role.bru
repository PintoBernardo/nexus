meta {
  name: Delete Role
  type: http
  seq: 8
}

delete {
  url: http://127.0.0.1:8000/api/admin/auth/roles/:name
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 5000
}

vars:path {
  name: manager
}
